# Don't put duplicate lines in the history. See bash(1) for more options.
# Don't overwrite GNU Midnight Commander's setting of `ignorespace'.
export HISTCONTROL=$HISTCONTROL${HISTCONTROL+,}ignoredups
# ... or force ignoredups and ignorespace
export HISTCONTROL=ignoreboth

export HISTSIZE=500000
export HISTFILESIZE=100000

# Let's ignore some commands.
export HISTIGNORE="&:[ ]*:exit:\:q:\:x:ls:la:ll:bg:fg:history:clear:jobs"

export GREP_OPTIONS='--color=auto'

# \033[36m = Begin cyan coloring (foreground).
# \033[m   = End coloring.
#export PS1='\w:\033[36m$(git branch 2> /dev/null | grep "^*" | colrm 1 2)\033[m $ '
export PS1='\w:$(git branch 2> /dev/null | grep "^*" | colrm 1 2)$ '

# For engineering bash scripts.
export WEBSERVER=/usr/local/www
export PYTHONPATH=$PYTHONPATH:/usr/local/bin

# Use vim as default editor instead of nano.
export EDITOR=vim

export NODE_PATH=/usr/local/lib/jsctags:${NODE_PATH}
export GOPATH="$HOME/sandbox/go"
export GOROOT=/usr/local/src/go
export GOROOT_BOOTSTRAP=/usr/local/src/go1.4
#export PATH="$GOPATH/bin:$HOME/npm/bin:/usr/local/go/bin:$PATH"
export PATH="/usr/local/src/go/bin:$GOPATH/bin:$HOME/bin:$HOME/npm/bin:$PATH"
export GITHOOKS=/usr/local/src/git/git/hooks

# To make use of gpg-agent when not using X, uncomment `use-agent`
# in ~/.gnupg/gpg.conf.
#eval "$(gpg-agent --daemon)"
#export GPG_TTY=$(tty)

