# shellcheck disable=1090

# If not running interactively, don't do anything.
[ -z "$PS1" ] && return

if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
fi

export GOPATH=/home/btoll/projects/veriforce/pecteam/devops/terraform-compliancepro/_go_src
export GOBIN=/home/btoll/projects/veriforce/pecteam/devops/terraform-compliancepro/_go_bin
